; PHP upload settings
; Videos no longer pass through the server (direct R2 upload).
; These limits only apply to small payloads like attachments and API requests.

upload_max_filesize = 50M
post_max_size = 55M
memory_limit = 512M
max_execution_time = 120
